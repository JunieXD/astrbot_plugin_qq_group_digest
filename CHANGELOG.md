# 更新记录

## 0.2.4

- 修复私聊 `/群摘要 预览` 固定回复纯文本、忽略展示配置的问题；四种模式均复用正式摘要的排版，合并转发保留封面。
- 预览按发起私聊的机器人与管理员路由，使用共享发送队列、随机间隔及发送额度；支持配置的发送前降级，发送结果不明时停止且不重发。
- 预览正文不再受管理员命令的 3800 字截断；保留展示模式自身的长度限制及错误提示。

## 0.2.3

- 新增 ECNU 思考模式和思考程度配置，默认开启 `low`；支持关闭或跟随共享模型设置。
- 按 Max / Plus 校验支持的档位，通过独立请求副本传参；开启思考时使用默认采样参数。
- 参数变化会隔离摘要结果缓存，统计记录实际请求设置；只使用最终答案，不保存或发送思考正文。

## 0.2.2

- 卡片封面改用 ✨，保留标题空行与分点排版。
- 模型分别生成具体对象、关键进展及要点，插件组合“对象｜进展”标题与分点正文；概述信息、合并同一人的相关发言，舍弃无法确定对象的片段，兼容旧摘要缓存。
- 首次摘要和私聊预览也读取配置的重叠上下文，避免时间边界切掉话题对象；背景不会扩大展示的本期时间范围。
- 修正关闭署名时，提示词末尾仍要求署名的冲突。

## 0.2.1

- 卡片封面增加 📰，标题与正文留白，正文按短要点排版；旧的整段摘要也按完整句子分段。
- 不再向群成员展示技术说明、预算说明和“未经核实”等固定尾注；保留署名及原有的“听说、可能”等限定。
- 提示词要求每条聚焦具体学校或事件，补充与反驳分别成段；更新配置说明。

## 0.2.0

- 优先整段单次调用：紧凑发言者代号、时间与消息编号、ECNU 上下文自动适配；超预算才分块，并保留回复及相邻上下文。
- 修复轻微超字数导致整个预览失败：按预算保留完整主题并提示省略数量；极端单条超限才定向改写。细分 JSON、来源、结构和字数错误。
- ECNU 请求隔离 JSON 模式、思考和输出设置，关闭隐式重试；按输入中的发言者代号恢复真实历史昵称，区分成员同名与改名，避免模型从长表里抄错署名。
- 转发在翻页中及时展开，缓存展开结果，必要时恢复原生消息定位；成功模型分块独立缓存，避免失败重试重复工作。
- 参考群审核插件加入持久化 LLM 调用、tokens、原始 usage、费用及峰谷计价、详情和 CSV 导出。
- 补充模型进度、接口耗时、缓存与校验诊断；增加已认证、无发送副作用的预览 API。
- 默认每页 100 条；慢查询会增加下一次等待。升级兼容既有进度数据库。


## 0.1.4 — 2026-09-25

- 新增预览与定时摘要共用的历史查询缓存，默认有效 30 分钟，可配置 0～120 分钟。
- 复用已完整读取的旧区间，重查新增消息及末尾一分钟；账号、群、接入或昵称采集设置变化时不混用缓存。
- 新增“预览 群号 刷新”命令；缓存不因重复命中无限续期，不复用旧翻页游标，不缓存未完成的读取或模型答案。
- 增加撤回消息刷新、读取中断、缓存过期、重启恢复、数据隔离与当前限制的回归测试。

## 0.1.3 — 2026-09-25

- 修复当前 AstrBot 中停止事件后，私聊命令的结果和错误提示被回复管道拦截；改为直接回复当前私聊。
- 预览受理后立即确认，状态命令显示读取页数、消息数、生成阶段及耗时；拒绝重复请求时不会误报已受理。
- 增加预览开始、完成、失败、取消和分页进度日志，错误或取消后清理进度状态。
- 新增管理员回复、初始化失败、预览受理及异常结果的回归测试。

## 0.1.2 — 2026-09-25

- 新增按群选择“引用群友说法时保留昵称”，使用历史中的群名片或昵称，不额外查询成员资料。
- 分块提炼及合并保留发言归属，区分同名成员、成员改名、缺失昵称和转发署名；开启此选项后重试会重新读取旧快照。
- 转发节点独立标记，避免把转发内容误算成转发者本人观点；为传闻、个人反馈和不同说法提供署名摘要示例。

## 0.1.1 — 2026-09-25

- 修复生成失败后重试仍使用旧配置；转发读取设置变化时重新读取，已生成内容保持固定。
- 修复移除目标群会丢失待核对状态；状态页优先显示旧的未解决批次，支持跳过失败生成。
- 修复查询失败冷却没有阻止后续查询，以及翻页中途重连仍可能复用旧游标。
- 修复摘要在队列或发送准备期间过期后仍可能发送；已有待发卡片采用最新的普通消息备用设置。
- 修复定时器清理被取消时重载可能挂起，以及清理异常导致数据库或实例锁未释放。
- 修复过度归一化使 C/C++、大小写不同的链接等内容被误判为重复。
- 提前阻止超量读取，停止轮询仅剩待核对的旧批次，避免非管理员命令触发自动回复。
- 修复显式 QQ 绑定被其他离线接入干扰，以及手动重试后仍受旧批次退避时间阻塞。
- 新增异常恢复、配置变更、连接切换、权限和资源释放的回归测试。

## 0.1.0 — 2026-09-25

- 新增按来源群配置的定时摘要任务，复用 AstrBot 模型和 NapCat 连接。
- 支持普通整篇、普通分条、合并转发整篇和合并转发分条。
- 支持首次回溯、时间重叠、停机补报和可选转发文字展开。
- 新增分页完整性检查、模型结构与来源校验、分块提炼和输入上限。
- 新增摘要及每个目标分段的持久进度、发送意图、未知状态核对和安全跳过。
- 新增共享操作队列兼容、随机等待、滚动额度、连接身份校验与重连冷却。
- 新增私聊管理员预览和管理命令、中文配置、日志轮转及数据保留设置。
- 新增跨平台 CI 和无需真实 QQ/模型服务的自动化测试。
