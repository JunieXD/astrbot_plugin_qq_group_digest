# 0.2.0 验证记录（2026-09-25）

本次使用本机 AstrBot、NapCat 和已配置的 ECNU Max 实测。聊天正文、模型输出、账号信息、密钥及运行数据库均未提交到仓库。

## 自动化

`uv run pytest -q`：199 项通过；`uv run ruff check .`、格式检查、`git diff --check` 通过。覆盖分页与缓存、重连、转发定位恢复、上下文分块、署名与改名、预算、重试记账、取消、统计导出、管理员权限、预览不发群消息及既有投递状态恢复。

## 真实链路

| 项目 | 结果 |
| --- | --- |
| 全新历史读取 | 5,659 条消息，58 页，205.8 秒 |
| 转发 | 6 次查询成功；嵌套转发未继续展开时保留说明 |
| 本机模型调用 | 1 次，19.2 秒，104,576 输入 / 1,929 输出 tokens |
| 最终摘要 | 11 个完整主题，标题正文共 2,432 字；按 2,600 字预算省略 1 条并告知 |
| 调用记录 | 状态 success，保存原始输出、usage、模型、阶段、耗时和计价快照 |
| 缓存用量缺失 | 缓存 tokens 为未知；费用记录上下界，不当作零消耗 |
| 重载后复用缓存 | 复用旧区间，只查询 1 页；5,639 条本期消息，历史读取 0.625 秒，6 个转发全部命中缓存 |
| 缓存后的新时间窗口 | 1 次模型调用，35.8 秒，12 个主题 |
| 真实投递 | 管理员私聊普通消息、13 节点合并转发均取得回执；重新展开卡片，13 个节点文字逐一完全一致 |

两次实机预览都通过已认证接口运行安装后的插件，使用实际 AstrBot 模型适配器和 NapCat 连接。固定历史样本另行回放验证了大量消息仍可一次调用；相同样本的旧输入约 89.4 万字符，紧凑输入约 26 万字符。对完全相同输入的已完成模型结果复用由自动化测试验证；第二次实机预览的时间窗口已变化，因此仍有一次新模型调用。

## 失败路径和边界

旧日志没有保存失败当次的原始模型输出。使用真实历史复现了同类失败：模型输出 2,638 字超过 2,600 字限制，重写后仍为 2,769 字。新版把发布预算与结构校验分开，保留完整主题；单条内容独自超过全部预算时才定向改写。

署名以模型引用的发言者代号恢复历史昵称，校验代号属于本次输入；这不是逐句事实核验。摘要内容仍可能被模型误解，群友反馈与传闻应保留归因和限定，不被当成核实的院校政策。

图片、语音和视频正文未识别，嵌套或超限转发会有说明。测试投递在管理员私聊进行，未启用生产群的定时发送；本机原有总开关保持关闭。
